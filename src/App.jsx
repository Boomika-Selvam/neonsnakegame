import React from 'react'
import Home from './pages/Home.jsx'

/**
 * App — root component. Keeps it minimal; all logic lives in Home + children.
 */
export default function App() {
  return <Home />
}
